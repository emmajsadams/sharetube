import 'fetch-mock'
/*
fetchMock.mock(url, 'GET', Promise.resolve(payload))

import 'sinon-as-promised'
import fetchMock from 'fetch-mock'
fetchMock.mock(url, 'GET', Promise.resolve(payload))
const videos = proxyquire('./videos', {
  'isomorphic-fetch': fetchMock.fetchMock
})

*/


/*

*/
